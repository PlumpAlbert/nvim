return {
  window = {
    position = 'right',
    width = 25,
  },
  filesystem = {
    follow_current_file = true,
    group_empty_dirs = true,
    filtered_items = {
      hide_gitignored = true,
    }
  }
}
